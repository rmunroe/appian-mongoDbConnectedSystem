function.mdb_not.description=Inverts the effect of a query clause and returns documents that do NOT match the query clause
function.mdb_not.param.queryClause.description=The expression (often created with other mdb_* functions) you wish to invert using the $not operator